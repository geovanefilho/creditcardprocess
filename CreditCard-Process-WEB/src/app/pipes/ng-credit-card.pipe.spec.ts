import { NgCreditCard } from './ng-credit-card.pipe';

describe('NgCreditCard', () => {
  it('create an instance', () => {
    const pipe = new NgCreditCard();
    expect(pipe).toBeTruthy();
  });
});
